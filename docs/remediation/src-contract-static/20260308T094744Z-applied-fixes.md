# Applied Fixes — Static Contract Remediation

- Timestamp: `20260308T094744Z`
- Scope: static contract parity and drift prevention guardrails

## Phase 2 (`kwargs` Entry-Point) Status

No new dispatcher code changes were required in this run; the latest source already includes:

- strict legacy-envelope normalization failures for malformed payloads,
- strict unknown/missing key validation before handler invocation,
- internal-only compatibility behavior in `TelecomBrowserApp.dispatch(...)`.

Validated by:

- `tests/unit/test_tool_invocation_compatibility.py`

## Phase 3/4/5 Fixes Applied

1. Added canonical registration-pattern static tests:
- `tests/contracts/test_registration_pattern_static.py`
- Covers:
  - unique canonical registration site,
  - direct `server.tool(name=tool_name)(handler)` binding shape,
  - no nested synthetic `**kwargs` wrappers in registration path.

2. Added static contract guardrail script:
- `scripts/check_static_contract_guardrails.sh`
- Enforces:
  - single registration site in server layer,
  - no `**kwargs` wrapper defs in server transport modules,
  - `"kwargs"` envelope usage restricted to `server/app.py`,
  - no raw `handler(**payload|kwargs)` forwarding patterns.

3. Expanded contract CI checks:
- `.github/workflows/tool-contracts.yml`
- Added tests:
  - `tests/contracts/test_registration_pattern_static.py::test_canonical_registration_site_is_unique`
  - `tests/contracts/test_registration_pattern_static.py::test_register_function_binds_direct_handler`
  - `tests/contracts/test_registration_pattern_static.py::test_register_function_has_no_nested_kwargs_wrapper`
  - `tests/contracts/test_tool_contract_parity.py::test_no_tool_publishes_synthetic_kwargs_field`
  - `tests/unit/test_tool_invocation_compatibility.py::test_dispatch_does_not_forward_nested_kwargs_key`
- Added job:
  - `tool_contract_static_guardrails` running `bash scripts/check_static_contract_guardrails.sh`

## Drift Categories Addressed

- synthetic envelope drift: guarded and statically checked
- wrapper signature drift: guarded by direct-binding + AST/static tests
- extra-property drift: covered by strict schema tests in contract suite
- required/name/type drift: covered by signature-schema parity tests

## Local Validation (Static)

Executed:

```bash
bash scripts/check_static_contract_guardrails.sh
.venv/bin/pytest -q tests/contracts/test_registration_pattern_static.py tests/contracts/test_tool_contract_parity.py tests/unit/test_tool_invocation_compatibility.py
```

Result:

- guardrail script: PASS
- tests: `18 passed`
