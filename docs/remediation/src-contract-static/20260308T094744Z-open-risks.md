# Open Risks and CI Guardrails

- Timestamp: `20260308T094744Z`

## Residual Risks

1. Legacy envelope compatibility remains in internal dispatch helper:
- Location: `src/telecom_browser_mcp/server/app.py`
- Risk: future internal callers may overuse deprecated `{ "kwargs": ... }` style.
- Mitigation: strict argument validation + explicit docs + unit tests.

2. Pattern-based guardrails can produce false positives/negatives:
- Location: `scripts/check_static_contract_guardrails.sh`
- Risk: regex policies may need updates if code structure evolves.
- Mitigation: pair regex checks with AST/static tests in `tests/contracts/test_registration_pattern_static.py`.

3. CI path filtering can skip checks for docs-only PRs that affect contract guidance:
- Location: `.github/workflows/tool-contracts.yml`
- Risk: documentation drift may bypass contract workflow.
- Mitigation: keep review checklist requiring contract-doc consistency.

## Static CI Guardrails Added

- contract parity tests (schema/signature)
- no-arg strictness tests (`kwargs` envelope rejection)
- canonical registration pattern tests
- grep/policy guardrail script for disallowed drift patterns

## Documented Exception

Intentional exception retained:

- internal-only deprecated legacy envelope handling in `TelecomBrowserApp.dispatch(...)`

Why it does not break public contract:

- FastMCP public tool schema is derived from explicit bound handler signatures.
- Registration path does not expose top-level `kwargs` field.

Protection tests:

- `tests/contracts/test_tool_contract_parity.py`
- `tests/unit/test_tool_invocation_compatibility.py`
- `tests/contracts/test_registration_pattern_static.py`
