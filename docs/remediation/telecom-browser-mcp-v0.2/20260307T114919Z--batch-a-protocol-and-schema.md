# Batch A Protocol and Schema

Changes:
- Tool catalog remains complete and contract-tested.
- Added/kept strict schema envelope behavior for all added handlers.
- Added tool discovery contract assertion test.

Rerun:
- `python -m pytest -q`
- `python -m ruff check src tests scripts`

Status: fixed
