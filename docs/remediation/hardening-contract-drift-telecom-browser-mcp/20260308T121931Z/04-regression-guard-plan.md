# 04 Regression Guard Plan

- RG-01: lifecycle transition tests (`mark_broken`) in unit suite.
- RG-02: redaction corpus expanded with `Authorization: Bearer`, `api_key`, `secret`.
- RG-03: host-required CI lane executes and requires at least one passing host test.
