# Static Source Map (`src/`)

- Timestamp: `20260308T094237Z`
- Scope: static inspection only (no runtime execution)
- Target: `src/telecom_browser_mcp/`

## Module Tree

```text
src/telecom_browser_mcp/
  __init__.py
  __main__.py
  adapters/
  browser/
  config/
  diagnostics/
  evidence/
  inspectors/
  models/
  resources/
  server/
  sessions/
  tools/
  validation/
  architecture_guardrails.py
  closed_loop.py
  failure_priority_scorer.py
  failure_signature_extractor.py
  failure_signature_matcher.py
  live_verification.py
  mcp_handshake_probe.py
  pipeline_governor.py
  remediation_batch_recommender.py
  runtime_environment_detector.py
  signature_library_store.py
  signature_regression_detector.py
  signature_signal_normalizer.py
  stability_governor.py
  system_drift_detector.py
```

## Package Boundaries (Observed)

- `server/`: MCP bootstrap, transport startup, tool registration, internal dispatch helper.
- `tools/`: telecom-intent tool handlers (`ToolOrchestrator`).
- `sessions/`: session lifecycle state and browser-session registry.
- `adapters/`: app-specific telecom UI logic and adapter contracts.
- `inspectors/`: runtime read-only extraction (store/SIP/WebRTC/environment).
- `diagnostics/`: failure interpretation/classification helpers.
- `evidence/`: artifact writers, markdown reports, bundle metadata.
- `models/`: output envelopes and typed domain snapshots.
- `validation/`: offline validation enrichment/classification utilities.

## Likely MCP Bootstrap Files

- `src/telecom_browser_mcp/__main__.py:1-4`
  - Entrypoint imports `server.stdio_server.main`.
- `src/telecom_browser_mcp/server/stdio_server.py:106-123`
  - Creates `FastMCP("telecom-browser-mcp")`, registers tools, runs stdio transport.
- `src/telecom_browser_mcp/server/sse_server.py:8-25`
  - Creates FastMCP and runs SSE transport.
- `src/telecom_browser_mcp/server/streamable_http_server.py:8-27`
  - Creates FastMCP and runs streamable HTTP transport.

## Likely Tool Modules

- `src/telecom_browser_mcp/tools/orchestrator.py`
  - Contains all exposed telecom-intent tool handlers (`health`, `list_sessions`, `open_app`, call-control, diagnostics, evidence tools).

## Likely Registry / Dispatch Modules

- Registration: `src/telecom_browser_mcp/server/stdio_server.py:46-59`
  - `_register_tools_with_fastmcp(server, app)` loops over `TOOL_NAMES` and registers handlers with `server.tool(name=tool_name)(handler)`.
- Internal dispatch helper: `src/telecom_browser_mcp/server/app.py:72-78`
  - `dispatch(tool_name, **kwargs)` resolves orchestrator handler, normalizes legacy envelope, validates arg keys, invokes handler.

## Likely Orchestrator / Session Modules

- Orchestrator: `src/telecom_browser_mcp/tools/orchestrator.py:21+`
  - Centralized business orchestration and response envelope creation.
- Session manager: `src/telecom_browser_mcp/sessions/manager.py`
  - `create_session`, `list_sessions`, `get`, `close_session`, `reset_session`.
- Session object: `src/telecom_browser_mcp/sessions/browser_session.py`
  - Browser session state and lazy `PlaywrightDriver` acquisition.

## Import-Graph Highlights (Contract-Relevant)

- `server/stdio_server.py` imports `TelecomBrowserApp` and binds FastMCP tool metadata.
- `server/app.py` imports `ToolOrchestrator` and handles internal dynamic dispatch.
- `tools/orchestrator.py` depends on adapters/sessions/diagnostics/models; no MCP registration logic inside orchestrator.

## Static Notes

- Tool contract surface is centralized through `TOOL_NAMES` in `stdio_server.py`.
- No separate `tool_registry.py` exists under `src/`.
- Canonical MCP contract path is registration via bound orchestrator methods (no synthetic wrappers in current source).
