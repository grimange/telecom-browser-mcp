# Troubleshooting

Common bootstrap failure classes:

{{KNOWN_BOOTSTRAP_FAILURES}}

If browser launch fails in sandboxed CI, classify as environment limitation unless host runtime evidence confirms a code defect.
