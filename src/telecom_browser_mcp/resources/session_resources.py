"""Optional resource schemas (minimal v1 scope)."""

SESSION_RESOURCE_URIS = [
    "session://{session_id}/registration",
    "session://{session_id}/active-call",
    "session://{session_id}/webrtc-summary",
]
