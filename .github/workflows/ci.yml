name: CI

on:
  push:
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Ruff
        run: ruff check src tests

      - name: Verify Contract Schemas
        run: |
          PYTHONPATH=src python scripts/generate_contract_schemas.py
          git diff --exit-code docs/contracts/m1

      - name: Pytest
        run: pytest -q

  host-e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          python -m playwright install chromium

      - name: Host Required E2E
        run: |
          pytest -q -m host_required -rA | tee /tmp/host-e2e.txt
          grep -Eq '[1-9][0-9]* passed' /tmp/host-e2e.txt
